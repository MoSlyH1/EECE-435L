Flask==2.0.3
Flask-SQLAlchemy==2.5.1
psycopg2==2.9.1  # Replace with the appropriate database driver if using a different database
Flask-WTF==0.15.1  # For handling forms in Flask
WTForms==2.3.3  # Form validation library
Flask-Migrate==3.1.0  # For database migrations with Flask
Flask-Bcrypt==0.7.1  # For hashing passwords in Flask
# Add any other dependencies as needed
